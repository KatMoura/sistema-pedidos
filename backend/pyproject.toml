[project]
name = "backend"
version = "0.1.0"
description = "Sistema de Pedidos - Backend com Padrao Observer"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "supabase>=2.0.0",
    "python-dotenv>=1.0.0",
]
